name: shell commands
on: [push]

jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
      - name: Echo a String
        run: eccho "Hello World 2"
      - name: Multiline Script
        run: |
          node -v
          npm -v